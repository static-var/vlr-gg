package dev.staticvar.designsystem.prism

/**
 * Supported design system theme variants.
 *
 * Each variant represents a complete brutalist theme with its own color palette,
 * typography, and visual styling.
 */
public enum class PrismVariant(public val displayName: String) {
  Light("Light"),
  Dark("Dark"),
}
